# 네이토 사전평가 결과

- 저장소: https://github.com/Anyoungju/CODYSSEY2-3
- 브랜치: `main`
- 점수: **100% (19/19)**

## 항목별 결과

### #1 — PASS

- 근거: README.md > '프론트엔드 데모: <https://anyoungju.github.io/CODYSSEY2-3/>'
- 잘한 점: 배포 URL을 README에 명시했고 내비게이션으로 섹션 이동을 구현함
- 부족한 점: 배포된 사이트의 실행 캡처(실제 HTTP 응답 스냅샷)가 README에 직접 포함되면 더 명확함
- 보완: README나 docs에 배포 접근 시의 스크린샷/HTTP 200 캡처를 추가해 배포 가시성을 높이세요.

### #2 — PASS

- 근거: css/styles.css > '@media(max-width:820px)'
- 잘한 점: 명확한 미디어쿼리(@media 820px/520px)로 모바일 레이아웃을 처리함
- 부족한 점: 모바일 동작 스냅샷은 있으나 추가 브라우저(다양한 뷰포트) 검증 로그가 있으면 좋음
- 보완: 추가 디바이스 크기별 스크린샷(예: 가로 모드, 태블릿)을 docs에 포함하세요.

### #3 — PASS

- 근거: js/app.js > 'fetch(getBlueprintEndpoint(), {method: "POST"' ; api/blueprint.py > 'from openai import OpenAI'
- 잘한 점: 프론트에서 POST 요청을 보내고 서버리스에서 OpenAI 클라이언트를 호출하는 흐름이 구현되어 있음
- 부족한 점: 실제 운영 환경에서 OpenAI 호출의 성공 응답 캡처(운영 로그)는 미첨부임
- 보완: 운영 Vercel 로그 혹은 실제 API 응답 스니펫을 추가해 AI 연동 동작을 입증하세요.

### #4 — PASS

- 근거: tests/test_blueprint.py > 'test_normalizes_optional_fields','test_rejects_empty_idea','test_rejects_long_idea'
- 잘한 점: 정상·빈·과도 입력(3가지 사례)에 대한 단위 테스트를 포함함
- 부족한 점: 사용자 관점에서의 재현 절차(브라우저에서의 긴 입력 시나리오 스크린샷)가 더 있으면 완성도 상승
- 보완: docs에 테스트 입력(정상/빈/긴)별 브라우저 캡처와 기대 메시지를 한 줄씩 추가하세요.

### #5 — PASS

- 근거: js/app.js > '아이디어를 한 문장 이상 입력해 주세요.'; api/blueprint.py > '아이디어를 입력해 주세요.'; js/app.js > '응답 시간이 길어졌어요. 잠시 후 다시 시도해 주세요.'
- 잘한 점: 빈 입력·서버 오류·타임아웃을 구분해 사용자 안내 메시지를 구현함
- 부족한 점: 각 상태에서의 UI 비활성화·재시도 흐름 예시(스크린샷/녹화)가 문서화되어 있지 않음
- 보완: 실패·지연 상황별 UI 스냅샷과 재시도 절차를 docs에 첨부하세요.

### #6 — PASS

- 근거: .env.example > 'OPENAI_API_KEY=your_openai_api_key' ; api/blueprint.py > 'if not os.getenv("OPENAI_API_KEY"):'
- 잘한 점: 환경변수 사용 예시와 서버에서 키를 확인하는 코드로 키 비노출 원칙을 지킴
- 부족한 점: 프로젝트에 실제 키가 커밋되었는지 자동 스캔 결과(실행 결과 스냅샷)는 문서화되지 않음
- 보완: CI에서의 시크릿 검사(스크립트 실행 결과)를 README에 요약해 추가하세요.

### #7 — PASS

- 근거: README.md > 'GitHub: <https://github.com/Anyoungju/CODYSSEY2-3>'
- 잘한 점: GitHub 저장소, README, 기획서(docs) 등 제출 패키지를 포함함
- 부족한 점: 운영 환경의 실제 호출 캡처(운영 OpenAI 응답)는 누락되어 있음
- 보완: 운영 호출(예: Vercel 로그 스니펫)이나 배포 스냅샷을 README 또는 docs/EVIDENCE.md에 추가하세요.

### #8 — PASS

- 근거: README.md > '프론트엔드는 OpenAI를 직접 호출하지 않으며, Python API는 화면 DOM을 알지 못합니다.'
- 잘한 점: 프론트·백엔드 경계와 이유(키 격리 등)를 명확히 설명함
- 부족한 점: 경계에 따른 배포·테스트 책임(누가 어디서 문제를 봐야 하는지)의 예시가 더 있으면 좋음
- 보완: 장애 시 각 계층에서 확인할 로그/콘솔 위치와 담당자 체크리스트를 간단히 추가하세요.

### #9 — PASS

- 근거: docs/SERVICE_PLAN.md > 'idle → validating → loading → success'
- 잘한 점: 프론트 상태 전이(로딩/성공/실패)와 상태 동작이 문서화되어 있음
- 부족한 점: 상태관리를 실제 코드(예: 상태 변수 분리·테스트)로 더 분명히 연계하면 유지보수성 향상
- 보완: js/app.js의 상태 처리(로딩 플래그, 에러 보존 등)를 짧게 매핑해 문서에 연결하세요.

### #10 — PASS

- 근거: api/blueprint.py > 'def validate_payload(payload: Any)'; api/blueprint.py > 'required = {"service_name", "one_liner", "pages", "ai_feature", "milestones", "risks"}'
- 잘한 점: 서버리스에서 입력 검증과 응답 스키마(필수 필드)를 코드에서 직접 검증함
- 부족한 점: 응답 포맷의 에지 케이스(부분 필드 누락 등)에 대한 예시 응답 샘플이 부족함
- 보완: docs에 오류별(400/413/502/503/504) 예시 JSON 응답을 요약해 추가하세요.

### #11 — PASS

- 근거: docs/OPERATIONS.md > 'vercel redeploy <deployment-url>'
- 잘한 점: Vercel 로그 확인과 재배포 절차, 문제 진단 방법을 문서화함
- 부족한 점: 실제 배포 사례(오류 로그 예시와 수정 이력)가 첨부되면 더 신뢰성 있음
- 보완: 최근 발생한 배포 오류 사례와 이를 해결한 로그 스니펫을 docs에 추가하세요.

### #12 — PASS

- 근거: README.md > 'HTML | 콘텐츠 의미 구조, 메뉴와 폼, 접근성 속성 | `index.html`'
- 잘한 점: HTML/CSS/JS 각각의 역할을 파일 단위로 명확히 서술함
- 부족한 점: 프론트 내 세부 책임(예: 어떤 책임을 JS로 이관했는지 파일별 예시)이 약간 더 상세하면 좋음
- 보완: 주요 파일마다 한 줄로 책임을 더 적어 README의 테이블을 보완하세요.

### #13 — PASS

- 근거: README.md > '사용자 입력 → JavaScript 검증 → POST /api/blueprint → Python 검증 → OpenAI Responses API → JSON 검사 → 결과 카드 렌더링'
- 잘한 점: fetch→서버리스→응답 반영의 전체 흐름을 문서로 정확히 설명함
- 부족한 점: 실제 fetch 실패 시 네트워크 에러와 HTTP 에러를 구분한 로그 예시가 없음
- 보완: 네트워크 실패/HTTP 5xx에 대한 브라우저 콘솔 예시와 복구 절차를 추가하세요.

### #14 — PASS

- 근거: docs/SERVICE_PLAN.md > '비밀키는 서버 환경변수에서만 읽으며 코드, 브라우저, 문서, 스크린샷에 기록하지 않는다.'
- 잘한 점: 환경변수 사용 이유(보안·운영)를 명확히 기술하고 정책을 제시함
- 부족한 점: 환경별(Preview/Production) 예시 값 관리 절차(권한 관리 등)가 더 상세하면 운영 안전성 향상
- 보완: 환경별 키 관리 체크리스트(누가 등록·교체하는지)를 운영 문서에 추가하세요.

### #15 — PASS

- 근거: README.md > '프롬프트는 코드의 `SYSTEM_PROMPT`를 v1 기준으로 관리한다.'
- 잘한 점: AI 도입 이유와 프롬프트 관리 관행(SYSTEM_PROMPT)까지 기술함
- 부족한 점: 프롬프트 버전별 비교(예: 결과 예시, 비용 차이)는 문서화가 제한적임
- 보완: 프롬프트 변경 시 기대 영향(응답 길이·토큰비용)을 간단한 표로 기록하세요.

### #16 — PASS

- 근거: docs/OPERATIONS.md > '더 작은 모델 | 지연·비용 감소 | 기획 구체성 감소 가능'
- 잘한 점: 지연 개선 옵션(모델 축소·입력 정규화·캐시 등)과 트레이드오프를 문서화함
- 부족한 점: 각 옵션의 예상 비용·지연 개선 수치(추정)가 없어서 선택 기준이 다소 정성적임
- 보완: 간단한 추정(예: 토큰 비용 비교, 응답 시간 목표)을 문서에 추가하세요.

### #17 — PASS

- 근거: docs/SERVICE_PLAN.md > '프론트 기능 증가: `js/app.js`를 `api-client.js`, `form-state.js`, `result-view.js`로 분리하고 URL 라우팅을 도입하고'
- 잘한 점: 프론트·백엔드 확장(모듈 분리, 캐시, 큐 등)에 대한 설계가 구체적임
- 부족한 점: 확장 시 마이그레이션 단계(작업 단위·리스크)가 더 구체적으로 제시되면 안전함
- 보완: 확장 작업을 단계별 체크리스트(파일별 변경 범위)를 추가해 마이그레이션 리스크를 낮추세요.

### #18 — PASS

- 근거: README.md > '1. OpenAI 대시보드에서 노출 키를 즉시 폐기합니다.'
- 잘한 점: 키 유출 대응 절차(즉시 폐기→재발급→교체→재배포 등)를 구체적으로 제시함
- 부족한 점: 대응 시나리오별(내부/외부 노출 등) 커뮤니케이션 템플릿이 없음
- 보완: 긴급 공지 템플릿(협업자·사용자 대상)을 운영 문서에 추가하세요.

### #19 — PASS

- 근거: docs/OPERATIONS.md > 'Vanilla 유지 | 빌드 없음, 작은 번들, 학습 구조 명확 | 화면·공유 상태가 늘면 수동 DOM 관리 증가'
- 잘한 점: 프레임워크 도입 영향(장단점·변경 범위)을 명확히 기술함
- 부족한 점: 프레임워크 전환 시 필요한 작업 목록(파일·테스트·CI 변경)이 더 상세하면 실무에 유용함
- 보완: 프레임워크 전환 체크리스트(컴포넌트 분해·번들링·테스트 업데이트)를 문서로 정리하세요.
